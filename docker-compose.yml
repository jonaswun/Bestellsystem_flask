services:
  backend:
    build: ./flask_app
    ports:                # Changed from expose to ports
      - "5000:5000"      # Correct port mapping syntax
    volumes:
      - flask_db_data:/flask_app/data
    environment:
      - FLASK_ENV=production
      - FOOD_PRINTER_IP=${FOOD_PRINTER_IP}
      - DRINKS_PRINTER_IP=${DRINKS_PRINTER_IP}
    networks:            # Add network configuration
      - app_network

  frontend:
    build: ./frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/certs:/etc/nginx/ssl:ro
    depends_on:
      - backend
    networks:           # Add network configuration
      - app_network

networks:              # Define the network
  app_network:
    driver: bridge

volumes:
  flask_db_data: