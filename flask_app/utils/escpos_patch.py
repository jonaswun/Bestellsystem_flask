# This file is intentionally empty.
# The DeviceNotFoundError class that used to live here was broken (referenced
# an undefined 'Error' base class) and was never imported anywhere in the
# codebase. It has been removed.